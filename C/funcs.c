#include <stdio.h>
#include <stdlib.h>

int pointer(int *a);

int main(){
    int y;
    
    int res = pointer(y);
    printf("The value is %d", res);
    return 0;
}

int pointer(int *a){
    printf("Meu penes");
}